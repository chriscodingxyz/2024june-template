import React, { useActionState } from "react";

export default function FormExample() {
  //   const [returnedFromAction, action, isPending] = useActionState(
  //     "action",
  //     null
  //   );

  return (
    <div>
      <form action=""></form>
    </div>
  );
}
